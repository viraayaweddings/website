node_modules
.next
.next-preview
.next-build
out
.env*
.scrape/
tsconfig.tsbuildinfo
npm-debug.log*

# local dev artifacts
*.log
.local-postgres/

# vendored venue images (9.7GB) — served via CDN/proxy at runtime, never committed
public/twc-venues-local/

# scratch / temp captures
data/venues/_*.html

# Photographer import artifacts (data lives in Neon; only the captured template is needed at runtime)
data/photographers/database.json
data/photographers/database.pretty.json
data/photographers/raw/
