{
  "buildCommand": "NITRO_PRESET=vercel npm run build && node --experimental-strip-types scripts/seed-static-pages.mjs --apply --if-configured",
  "installCommand": "npm install",
  "framework": null,
  "regions": [
    "sin1"
  ]
}
