/**
 * Present for tooling that expects it. The build runs through Vinext, which
 * does not read this file, and `next` itself is not a dependency -- so the
 * config is typed structurally rather than importing NextConfig from it.
 */
const nextConfig = {
  /* config options here */
};

export default nextConfig;
