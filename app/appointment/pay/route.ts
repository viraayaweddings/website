import { leadOptions, leadPost } from "../../lead-route";

export const dynamic = "force-dynamic";
export const runtime = "nodejs";

export const POST = leadPost("appointment");
export const OPTIONS = leadOptions;
